This sample project demonstrates a timer which counts in tenths of a second, along with some secondary timers that change its text color.

APIs used in this sample include:
timer.performWithDelay()
timer.pause()
timer.resume()
timer.cancel()
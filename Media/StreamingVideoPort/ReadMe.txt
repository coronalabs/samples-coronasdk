Demonstrates media.playVideo to stream a video from a remote source (URL).

This sample code is a portrait-only app and uses CoronaUseIOS6PortraitOnlyWorkaround in build.settings file to allow video to play in landscape mode on iOS. Android already supports portrait and landscape video modes.
This sample project demonstrates three widget buttons with different listener types:

The "onEvent" button detects all phases of user interaction.

The "onPress" button only detects initial touch.

The "onRelease" button only detects touch lifting off within its bounds.

RELATED DOCUMENTATION
[widget.newButton()](https://docs.coronalabs.com/api/library/widget/newButton.html)

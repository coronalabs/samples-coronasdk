Demonstrates how to create the native display objects TextFields, TextBoxes, Alerts, Activity Indicators, and WebPopUps. It uses the UI library to make buttons and labels too. The display will change according to which way the user turns the device. This is made possible with System orientation events. This application does not have helpInfo because the native display objects are always on top. 